// loops through each item in the range from the last character of the
// first string (d) up to the last character of the last string (m)
for(str in 'held'..'helm')
{
  print "${str} "
}

println ""
