str = 'hello'

println str

// the dump method returns the instance 
// of the class at runtime
// lets you take a peek into an object
// and displays the target instance, hash code, fields
println str.dump()
